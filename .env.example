# Flask Configuration
FLASK_DEBUG=False
FLASK_ENV=production

# Security - Generate a strong SECRET_KEY using: python -c "import os; print(os.urandom(24).hex())"
SECRET_KEY=your_secret_key_here_replace_with_strong_random_key

# Database (optional)
# DATABASE_URL=sqlite:///novastore.db

# Email (optional, for notifications)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your_email@gmail.com
# MAIL_PASSWORD=your_app_password
